type Movie = {
    imdbID: string
    title: string,
    genres: string[],
    directorName: string,
    duration: number,
    year: number,
    imdbScore: number,
    viewCounter: number,
}

export default Movie;