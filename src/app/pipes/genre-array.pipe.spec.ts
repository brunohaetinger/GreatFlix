import { GenreArrayPipe } from './genre-array.pipe';

describe('GenreArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
